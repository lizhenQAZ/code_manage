# 可变类型的序列赋值不改变内存位置
l = [1, 2, 3]
print(id(l))
l *= 2
print(id(l))

# 不可变类型序列赋值，需要改变内存位置
s = "1 2 3"
print(id(s))
s *= 2
print(id(s))

# 可变类型加入不可变类型
# python3.5不支持
t = (1, 2, [30, 40])
t[2] += [50, 60]
print(t)
